<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.Maui.Controls"
             x:Class="ScanPackage.CellScanPage"
             Title="Quét mã"
             BackgroundColor="Black">
    <Grid>
        <zxing:CameraBarcodeReaderView x:Name="BarcodeView"
                                        BarcodesDetected="OnBarcodesDetected"
                                        IsDetecting="True"
                                        CameraLocation="Rear"
                                        VerticalOptions="FillAndExpand"
                                        HorizontalOptions="FillAndExpand" />
        
        <!-- Overlay với hướng dẫn -->
        <Grid RowDefinitions="*,Auto">
            <!-- Top overlay với hướng dẫn -->
            <Grid Grid.Row="0" 
                  VerticalOptions="Start"
                  Padding="20,40,20,20">
                <Frame BackgroundColor="#80000000"
                       CornerRadius="12"
                       Padding="16"
                       HasShadow="False"
                       HorizontalOptions="Center">
                    <Label Text="📷 Hướng camera vào mã vạch&#10;Có thể quét ở mọi góc độ"
                           TextColor="White"
                           FontSize="16"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"
                           HorizontalTextAlignment="Center"/>
                </Frame>
            </Grid>

            <!-- Bottom controls -->
            <Grid Grid.Row="1" 
                  Padding="16,16,16,24"
                  BackgroundColor="#80000000"
                  VerticalOptions="End">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <Label Text="Chạm vào màn hình để quét mã vạch"
                       TextColor="White"
                       FontSize="14"
                       VerticalOptions="Center"
                       Grid.Column="0"/>
                
                <Button Text="✕ Đóng"
                        BackgroundColor="#F04E23"
                        TextColor="White"
                        FontSize="16"
                        FontAttributes="Bold"
                        CornerRadius="8"
                        Padding="20,12"
                        Clicked="OnCloseClicked"
                        Grid.Column="1"/>
            </Grid>
        </Grid>
    </Grid>
</ContentPage>









